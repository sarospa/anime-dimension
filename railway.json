{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m pip install requirements.txt && hypercorn app:app --bind \"[::]:$PORT\""
  }
}